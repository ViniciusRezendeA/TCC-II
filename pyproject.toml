[project]
name = "mcp-pipeline"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Vinicius Rezende Arantes De Araujo Moreira", email = "vinicius.rezende.ext@ipiranga.ipiranga" }
]
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.5",
    "python-dotenv>=1.2.3",
    "pyyaml>=6.0.3",
    "requests>=2.34.2",
    "tenacity>=9.1.4",
    "tree-sitter==0.25.2",
    "tree-sitter-c-sharp==0.23.5",
    "tree-sitter-java==0.23.5",
    "tree-sitter-javascript==0.23.1",
    "tree-sitter-python==0.23.6",
    "tree-sitter-typescript==0.23.2",
]

[project.scripts]
mcp-pipeline = "mcp_pipeline:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.3",
]

[tool.pytest.ini_options]
# Without this, pytest's default recursive discovery also scans data/repos/
# — real cloned MCP servers (see data/), which have their own test_*.py
# files that pytest tries to import as if they belonged to this project's
# suite. Found by hitting exactly that collection error after cloning real
# repos for Etapa 2 validation.
testpaths = ["tests"]
